# BBF-060 Editor Helper Surface

Date: 2026-05-23
Status: DONE

## Summary

Formalized the reusable example/editor helper surface as `configure_example_context`.

The helper now owns the repeated example setup:

- disable input trickling;
- toggle docking;
- build the default font atlas;
- disable `.ini` persistence.

All backend examples now call the shared helper instead of repeating the same initialization block.

## Gates

- `cargo +stable nextest run -p dear-imgui-bevy configure_example_context` - PASS, 1 test.
- `cargo +stable check -p dear-imgui-bevy --examples --features render` - PASS.
- `cargo +stable nextest run -p dear-imgui-bevy --features render` - PASS, 28 tests run and 2
  skipped.

## Notes

Keep `configure_example_context` small and example-focused. If editor-specific behavior grows again,
split a narrower follow-on instead of expanding the helper into a new framework.
