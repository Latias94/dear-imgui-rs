# BBP-050 Platform And CI Stabilization

Date: 2026-05-23

## Summary

Added a dedicated Bevy backend CI lane in `.github/workflows/ci.yml` and tightened the Bevy
backend README so its Rust/Bevy/wasm gates are clearly owned by that lane rather than the root
workspace workflow.

## Verification

- `cargo +stable fmt --all --check` - PASS.
- `cargo +stable check -p dear-imgui-bevy --examples --features render` - PASS.
- `cargo +stable nextest run -p dear-imgui-bevy --features render` - PASS, 29 tests run and 2
  skipped.
- `cargo +stable check -p dear-imgui-bevy --target wasm32-unknown-unknown --no-default-features`
  - PASS.
- `cargo +stable check -p dear-imgui-bevy --target wasm32-unknown-unknown --features render` - PASS.
- `git diff --check` - PASS.
- `ruby -e "require 'yaml'; YAML.load_file('.github/workflows/ci.yml'); puts 'workflow ok'"` - PASS.

## Notes

The dedicated job intentionally keeps the Bevy backend policy separate from the rest of the
workspace build matrix. If the support matrix expands again, split a further follow-on instead of
letting the lane grow implicitly.
