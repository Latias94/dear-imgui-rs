# BBP-060 - Split Multi-Viewport Implementation

Date: 2026-05-23
Status: DONE_WITH_SPLIT

## Summary

Closed this product follow-up lane as the implementation record for example organization, the safe
multi-viewport boundary slice, editor product surface, and platform/CI stabilization. Real Dear
ImGui docking multi-viewport OS-window support is now the next target and has been split into
`docs/workstreams/bevy-docking-multi-viewport-v1/`.

## Decision

The next lane starts with the core PlatformIO contract instead of Bevy code only. The user accepts
fearless `dear-imgui-rs` refactoring when the core API shape blocks a safe engine-managed backend.

## Verification

No code gates were rerun for this split task. The latest implementation evidence remains the BBP-050
gate set recorded in `EVIDENCE_AND_GATES.md`.
