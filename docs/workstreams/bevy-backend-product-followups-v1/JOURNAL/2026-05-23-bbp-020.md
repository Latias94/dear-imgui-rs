# BBP-020 Example Catalog

Date: 2026-05-23

## Summary

Organized the Bevy backend examples into purpose-based folders while preserving the public Cargo
example names:

- `examples/basic/simple.rs`
- `examples/runtime/windowed_overlay.rs`
- `examples/ecosystem/ecosystem.rs`
- `examples/ecosystem/bevy_plot_controls.rs`
- `examples/editor/editor_shell.rs`

The README now includes an example index with the source path, use case, and run command for each
example.

## Verification

- `cargo +stable fmt --all --check` - PASS.
- `cargo +stable check -p dear-imgui-bevy --examples --features render` - PASS.

## Notes

This slice changes example organization and documentation only. It does not change backend runtime
behavior, so the broader render-feature nextest package gate is left for behavior-changing slices.
