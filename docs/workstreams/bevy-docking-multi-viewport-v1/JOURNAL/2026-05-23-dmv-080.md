# DMV-080 - Closeout

Date: 2026-05-23
Status: DONE

## Summary

- Closed the Bevy docking multi-viewport lane.
- Verified the full requested native path: PlatformIO lifecycle capture, Bevy secondary `Window`
  entity mapping, input/focus/DPI/cursor/IME feedback, secondary viewport render routing, README
  support matrix, and product-facing `editor_shell` example command.
- Fixed a final closeout regression where plain `render` builds could treat an `ImguiViewportWindow`
  marker as a Dear ImGui platform viewport route even though `multi_viewport_supported` was false.
  Render extraction now collects viewport targets only when full native multi-viewport support is
  enabled.

## Verification

- `cargo +stable test -p dear-imgui-rs --features multi-viewport platform_io` - PASS.
- `cargo +stable nextest run -p dear-imgui-bevy --features render` - PASS, 34 tests run and 2
  skipped.
- `cargo +stable nextest run -p dear-imgui-bevy --features render,multi-viewport` - PASS, 39 tests
  run and 2 skipped.
- `cargo +stable check -p dear-imgui-bevy --examples --features render` - PASS.
- `cargo +stable check -p dear-imgui-bevy --features render,multi-viewport --example editor_shell`
  - PASS.
- `cargo +stable check -p dear-imgui-bevy --target wasm32-unknown-unknown --no-default-features` -
  PASS.
- `cargo +stable check -p dear-imgui-bevy --target wasm32-unknown-unknown --features render` -
  PASS.
- `cargo +stable check -p dear-imgui-bevy --target wasm32-unknown-unknown --features render,multi-viewport`
  - EXPECTED FAIL at the core unsupported-target compile gate.
- `cargo +stable fmt --all --check` - PASS.
- `git diff --check` - PASS.

## Review

- Workstream compliance: no blocking findings. All TODO items are complete, and final evidence
  covers core PlatformIO, Bevy package behavior, product-facing examples, wasm boundaries,
  formatting, and patch hygiene.
- Code quality: no blocking findings. The closeout render extraction fix keeps ordinary multi-window
  overlay routing separate from Dear ImGui platform viewport routing by checking
  `ImguiBackendStatus::multi_viewport_supported`.
- Residual risks: minimized-window feedback remains unavailable in Bevy `0.19.0-rc.2`; wasm
  multi-viewport and mobile multi-window support need separate platform lanes.
