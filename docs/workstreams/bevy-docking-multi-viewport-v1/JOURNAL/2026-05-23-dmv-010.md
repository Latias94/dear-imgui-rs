# DMV-010 - Scope Freeze

Date: 2026-05-23
Status: DONE

## Summary

Opened `docs/workstreams/bevy-docking-multi-viewport-v1/` as the dedicated execution lane for real
Dear ImGui docking multi-viewport OS-window support in the Bevy backend.

The previous product follow-up lane only completed the safe boundary slice: requests are visible and
support is explicitly false. This lane owns the actual implementation.

## Decisions

- Use a queued bridge between C ABI PlatformIO callbacks and Bevy ECS systems.
- Allow core `dear-imgui-rs` refactoring when existing APIs make the Bevy engine contract unsafe or
  awkward.
- Keep `ConfigFlags::VIEWPORTS_ENABLE` disabled until lifecycle, status, input feedback, and render
  routing are proven.

## Verification

No code gates were run. This task created planning and evidence documents only.
