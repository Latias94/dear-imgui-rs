# DMV-070 - Example, Docs, And Product-Facing Proof

Date: 2026-05-23
Status: DONE

## Summary

- Updated `backends/dear-imgui-bevy/examples/editor/editor_shell.rs` to request
  `ImguiBackendConfig::multi_viewport = true` only when the `multi-viewport` Cargo feature is
  compiled in.
- Kept the existing `render` example gate valid, so the editor shell still compiles without native
  multi-viewport support.
- Added Diagnostics panel status lines for `multi_viewport_requested`,
  `multi_viewport_supported`, and viewport render routing.
- Updated `backends/dear-imgui-bevy/README.md` with the native
  `render,multi-viewport` editor shell command and the wasm boundary.

## Verification

- `cargo +stable check -p dear-imgui-bevy --features render --example editor_shell` - PASS.
- `cargo +stable check -p dear-imgui-bevy --features render,multi-viewport --example editor_shell`
  - PASS.
- `cargo +stable check -p dear-imgui-bevy --examples --features render` - PASS.
- `cargo +stable fmt --all --check` - PASS.
- `git diff --check` - PASS.

## Notes

- This task intentionally did not change `editor_shell`'s Cargo `required-features`; users can still
  compile the example with `render` only.
- The native OS-window path is exercised by the documented `render,multi-viewport` command.
