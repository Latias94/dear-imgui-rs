# BEVY-130 — Ecosystem Composition Example

Implemented the shared-frame ecosystem composition example.

## Completed Slice

- Added `backends/dear-imgui-bevy/examples/ecosystem.rs`.
- The example initializes ImPlot, ImNodes, and ImGuizmo as non-send Bevy resources and drives them from the same `ImguiPrimaryContextPass`.
- Updated the backend README with the ecosystem example run command.

## Validation

- `cargo +stable check -p dear-imgui-bevy --example ecosystem` — PASS.
- `cargo +stable nextest run -p dear-implot -p dear-imnodes -p dear-node-editor -p dear-imguizmo` — PASS, 84 tests.

## Notes

This proves shared-frame composition inside the Bevy backend. It does not attempt to turn the crate into a full editor product.
