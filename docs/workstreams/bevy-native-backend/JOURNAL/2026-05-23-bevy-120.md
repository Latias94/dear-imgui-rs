# BEVY-120 — Editor Shell Example

Implemented the editor-oriented render-feature example.

## Completed Slice

- Added `backends/dear-imgui-bevy/examples/editor_shell.rs`.
- The example creates a Bevy render-target `Image`, registers it through `ImguiBevyTextures`, shows it inside a dockspace-driven ImGui shell, and records the editor input routing policy.
- Marked the example as `required-features = ["render"]` and updated the backend README with the run command.

## Validation

- `cargo +stable check -p dear-imgui-bevy --features render --example editor_shell` — PASS.
- `cargo +stable fmt --all` — PASS.

## Notes

This is still a proof example, not a full editor product. Keep inspector/assets/console as follow-ons unless they are required for the next lane.
