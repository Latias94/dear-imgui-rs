[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1.0"
bindgen = "0.72"
xshell = "0.2"
which = "8"
build-support = { package = "dear-imgui-build-support", path = "../tools/build-support", features = ["binding-spec"] }
